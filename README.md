# CWI Task
To implement the Baseline model run the 'runModel.py' file with the Base switch set to True.
To implement the models on the dev set run the 'runModel.py' with the test switch set to False.
